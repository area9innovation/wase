nested() -> (i64, (i32, f64)) {
    (1w, (2, 1.1));
}

piece() -> (i32, f64) {
    (3, 3.141);
}

export _start() -> () {
	t0 : (i64, (i32, f64)) = nested();
	t1 : (i64, (i32, f64)) = (1w, (2, 1.1));
	t2 : ((i32, f64), i64) = (piece(), 1w);
    // r3 : (i64, (i32, f64)) = if (true) t0 else (1w, (2, 1.1));
	{}
}
