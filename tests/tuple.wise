include lib/runtime;

/*
print2(t : (i32, f64)) -> () {
	printi32(t.0);
	fi : f64 = t.1;
	si : i32 = trunc_s<>(fi);
	printi32(si);
}
*/

mktuple() -> (i32, f64) {
	(1, 3.141)
}

export _start() -> () {
	t = mktuple();

	fi : f64 = t.1;
	si : i32 = trunc_s<>(fi);

	printi32(si);

//	print2( (1, 2.2) );

	{}
}
