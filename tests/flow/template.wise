include lib/wise/runtime;
include tools/flowc/backends/wise/lib/tuple;
include tools/flowc/backends/wise/lib/array;
include tools/flowc/backends/wise/lib/string;

 addT[A](v : A) -> A {
	
	v
}

 mapT[A](arr : A, fn : (A) -> A) -> A {
	
	fn(arr)
}

 myFnTemplate2[A,B](value1 : A, value2 : B) -> () {
	
	()
}

 export _start() -> f64 {
	initMemory();
	a : i32 = {{
	0;
	tmp0 : i32 = {createArray[i32](1)};
	
	setValueArray[i32](tmp0, 0, 1);
	tmp0;
	;
	}};
	{
	myFnTemplate2[i32, f64](1, 1.1);
	myFnTemplate2[f64, i32](2.2, 2);
	myFnTemplate2[i32, f64](a, 3.3);
	mapT[i32](10, addT[i32]);
	mapT[f64](0.45, addT[f64]);
	}
}
